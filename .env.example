# Example environment variables for Mail Blast
# These are optional — the app uses the UI to accept sender credentials.

# Server port
PORT=3000

# (Optional) If you want to configure a default sender on the server, set these.
# Note: It's safer to enter credentials via the UI at runtime.
SMTP_SENDER_EMAIL=
SMTP_APP_PASSWORD=
